# SalesHandovers Collection

> Generated by @fyit/crouton-cli on 2026-08-03

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `salesHandovers` |
| Layer | `sales` |
| API Base | `/api/teams/[teamId]/sales-handovers` |
| Form Component | `<SalesHandoversForm />` |
| List Component | `<SalesHandoversList />` |
| Composable | `useSalesHandovers()` |

## Features

- References: eventId → events, orderId → orders

## File Structure

```
collections/handovers/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useSalesHandovers.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/sales-handovers/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [handoverId].patch.ts    # UPDATE
│   │   └── [handoverId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| eventId | string (→ events) | Yes |
| orderId | string (→ orders) | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/sales-handovers

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/sales-handovers`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/sales-handovers?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/sales-handovers

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/sales-handovers`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/sales-handovers/{handoverId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/sales-handovers/${handoverId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/sales-handovers/{handoverId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/sales-handovers/${handoverId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: handovers, pending } = await useCollectionQuery('salesHandovers')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('salesHandovers')
</script>

<template>
  <SalesHandoversList />
</template>
```

### Schema & Validation

```typescript
import { salesHandoverSchema } from './app/composables/useSalesHandovers'

// Validate data
const result = salesHandoverSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only handovers`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useSalesHandovers.ts`:
```typescript
export const salesHandoverSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/sales-handovers/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
