<!--
  @crouton-generated
  @collection bookmarks
  @layer main
  @generated 2026-07-08

  ## AI Context
  - List component for bookmarks collection
  - Collection name: mainBookmarks
  - API endpoint: /api/teams/[id]/main-bookmarks
  - Columns composable: useMainBookmarks()
  - Column fields: title, url, tags, notes

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="mainBookmarks"
    :columns="columns"
    :rows="bookmarks || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="MainBookmarks"
        :collection="'mainBookmarks'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useMainBookmarks from '../composables/useMainBookmarks'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useMainBookmarks()

const { items: bookmarks, pending } = await useCollectionQuery(
  'mainBookmarks'
)

</script>