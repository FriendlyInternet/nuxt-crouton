/**
 * @crouton-generated
 * @collection bookmarks
 * @layer main
 * @generated 2026-07-08
 *
 * ## AI Context
 * - Composable: useMainBookmarks
 * - Collection name: mainBookmarks
 * - API endpoint: /api/teams/[id]/main-bookmarks
 * - Form component: MainBookmarksForm
 * - List component: MainBookmarksList
 * - Fields: title, url, tags, notes
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const mainBookmarkSchema = z.object({
  title: z.string().min(1, 'title is required'),
  url: z.string().min(1, 'url is required'),
  tags: z.string().optional(),
  notes: z.string().optional()
})

export const mainBookmarksColumns = [
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'url', header: 'Url' },
  { accessorKey: 'tags', header: 'Tags' },
  { accessorKey: 'notes', header: 'Notes' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _mainBookmarksConfig = {
  name: 'mainBookmarks',
  layer: 'main',
  apiPath: 'main-bookmarks',
  componentName: 'MainBookmarksForm',
  defaultValues: {
    title: '',
    url: '',
    tags: '',
    notes: ''
  },
  columns: mainBookmarksColumns,
  fields: [
      {
          "name": "title",
          "type": "string",
          "label": "Title",
          "area": "main"
      },
      {
          "name": "url",
          "type": "string",
          "label": "URL",
          "area": "main"
      },
      {
          "name": "tags",
          "type": "string",
          "label": "Tags",
          "area": "main"
      },
      {
          "name": "notes",
          "type": "text",
          "label": "Notes",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_mainBookmarksConfig, 'schema', {
  value: mainBookmarkSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const mainBookmarksConfig = _mainBookmarksConfig as typeof _mainBookmarksConfig & { schema: typeof mainBookmarkSchema }

export const useMainBookmarks = () => mainBookmarksConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: mainBookmarksConfig.defaultValues,
    schema: mainBookmarkSchema,
    columns: mainBookmarksConfig.columns,
    collection: mainBookmarksConfig.name
  }
}