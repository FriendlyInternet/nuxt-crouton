/**
 * @crouton-generated
 * @collection bookmarks
 * @layer main
 * @generated 2026-07-08
 *
 * ## AI Context
 * - Main interface: MainBookmark
 * - Form data type: MainBookmarkFormData
 * - New item type: NewMainBookmark
 * - Form props: MainBookmarkFormProps
 * - Fields: title, url, tags, notes
 *
 * ## Common Modifications
 * - Add field: Add to interface and ensure schema matches
 * - Change field type: Update both interface and schema
 * - Add computed field: Add optional property to interface
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import type { z } from 'zod'
import type { mainBookmarkSchema } from './app/composables/useMainBookmarks'

export interface MainBookmark {
  id: string
  teamId: string
  owner: string
  title: string
  url: string
  tags?: string
  notes?: string
  createdAt: Date
  updatedAt: Date
  createdBy: string
  updatedBy: string
  optimisticId?: string
  optimisticAction?: 'create' | 'update' | 'delete'
}

export type MainBookmarkFormData = z.infer<typeof mainBookmarkSchema>
// New* allows the server-set fields (id, createdBy, updatedBy) as optional — the
// generated POST endpoint provides them; other callers (e.g. seed) may omit them.
export type NewMainBookmark = Omit<MainBookmark, 'id' | 'createdAt' | 'updatedAt' | 'createdBy' | 'updatedBy'> & { id?: string; createdBy?: string; updatedBy?: string }

// Props type for the Form component
export interface MainBookmarkFormProps {
  items: string[] // Array of IDs for delete action
  activeItem: MainBookmark | Record<string, never> // MainBookmark for update, empty object for create
  collection: string
  loading: string
  action: 'create' | 'update' | 'delete'
}