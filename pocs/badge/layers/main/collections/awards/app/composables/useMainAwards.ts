/**
 * @crouton-generated
 * @collection awards
 * @layer main
 * @generated 2026-08-04
 *
 * ## AI Context
 * - Composable: useMainAwards
 * - Collection name: mainAwards
 * - API endpoint: /api/teams/[id]/main-awards
 * - Form component: MainAwardsForm
 * - List component: MainAwardsList
 * - Fields: name, points
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const mainAwardSchema = z.object({
  name: z.string().min(1, 'name is required'),
  points: z.number()
})

export const mainAwardsColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'points', header: 'Points' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _mainAwardsConfig = {
  name: 'mainAwards',
  layer: 'main',
  apiPath: 'main-awards',
  componentName: 'MainAwardsForm',
  defaultValues: {
    name: '',
    points: 0
  },
  columns: mainAwardsColumns,
  fields: [
      {
          "name": "name",
          "type": "string",
          "label": "Name",
          "area": "main"
      },
      {
          "name": "points",
          "type": "number",
          "label": "Points",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_mainAwardsConfig, 'schema', {
  value: mainAwardSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const mainAwardsConfig = _mainAwardsConfig as typeof _mainAwardsConfig & { schema: typeof mainAwardSchema }

export const useMainAwards = () => mainAwardsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: mainAwardsConfig.defaultValues,
    schema: mainAwardSchema,
    columns: mainAwardsConfig.columns,
    collection: mainAwardsConfig.name
  }
}