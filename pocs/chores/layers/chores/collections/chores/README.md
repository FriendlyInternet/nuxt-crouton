# ChoresChores Collection

> Generated by @fyit/crouton-cli on 2026-08-05

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `choresChores` |
| Layer | `chores` |
| API Base | `/api/teams/[teamId]/chores-chores` |
| Form Component | `<ChoresChoresForm />` |
| List Component | `<ChoresChoresList />` |
| Composable | `useChoresChores()` |

## Features

- References: assigneeId → users, lastDoneById → users

## File Structure

```
collections/chores/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useChoresChores.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/chores-chores/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [choreId].patch.ts    # UPDATE
│   │   └── [choreId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| cadence | string | Yes |
| assigneeId | string (→ users) | Yes |
| lastDoneById | string (→ users) | No |
| lastDoneAt | date | No |
| notes | text | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/chores-chores

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/chores-chores`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/chores-chores?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/chores-chores

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/chores-chores`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/chores-chores/{choreId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/chores-chores/${choreId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/chores-chores/{choreId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/chores-chores/${choreId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: chores, pending } = await useCollectionQuery('choresChores')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('choresChores')
</script>

<template>
  <ChoresChoresList />
</template>
```

### Schema & Validation

```typescript
import { choresChoreSchema } from './app/composables/useChoresChores'

// Validate data
const result = choresChoreSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only chores`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useChoresChores.ts`:
```typescript
export const choresChoreSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/chores-chores/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
