<!--
  @crouton-generated
  @collection chores
  @layer chores
  @generated 2026-08-05

  ## AI Context
  - List component for chores collection
  - Collection name: choresChores
  - API endpoint: /api/teams/[id]/chores-chores
  - Columns composable: useChoresChores()
  - Column fields: name, cadence, assigneeId, lastDoneById, lastDoneAt, notes

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="choresChores"
    :columns="columns"
    :rows="chores || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="ChoresChores"
        :collection="'choresChores'"
        createButton
      />
    </template>
    <template #assigneeId-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.assigneeId"
        :id="row.original.assigneeId"
        collection="choresUsers"
      />
    </template>
    <template #lastDoneById-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.lastDoneById"
        :id="row.original.lastDoneById"
        collection="choresUsers"
      />
    </template>
    <template #lastDoneAt-cell="{ row }">
      <CroutonDate :date="row.original.lastDoneAt" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useChoresChores from '../composables/useChoresChores'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useChoresChores()

const { items: chores, pending } = await useCollectionQuery(
  'choresChores'
)

</script>