/**
 * @crouton-generated
 * @collection chores
 * @layer chores
 * @generated 2026-08-05
 *
 * ## AI Context
 * - Composable: useChoresChores
 * - Collection name: choresChores
 * - API endpoint: /api/teams/[id]/chores-chores
 * - Form component: ChoresChoresForm
 * - List component: ChoresChoresList
 * - Fields: name, cadence, assigneeId, lastDoneById, lastDoneAt, notes
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const choresChoreSchema = z.object({
  name: z.string().min(1, 'name is required'),
  cadence: z.string().min(1, 'cadence is required'),
  assigneeId: z.string().min(1, 'assigneeId is required'),
  lastDoneById: z.string().optional(),
  lastDoneAt: z.coerce.date().nullish(),
  notes: z.string().optional()
})

export const choresChoresColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'cadence', header: 'Cadence' },
  { accessorKey: 'assigneeId', header: 'AssigneeId' },
  { accessorKey: 'lastDoneById', header: 'LastDoneById' },
  { accessorKey: 'lastDoneAt', header: 'LastDoneAt' },
  { accessorKey: 'notes', header: 'Notes' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _choresChoresConfig = {
  name: 'choresChores',
  layer: 'chores',
  apiPath: 'chores-chores',
  componentName: 'ChoresChoresForm',
  defaultValues: {
    name: '',
    cadence: '',
    assigneeId: '',
    lastDoneById: '',
    lastDoneAt: null,
    notes: ''
  },
  columns: choresChoresColumns,
  fields: [
      {
          "name": "name",
          "type": "string",
          "label": "Name",
          "area": "main"
      },
      {
          "name": "cadence",
          "type": "string",
          "label": "Cadence",
          "area": "main",
          "displayAs": "optionsSelect"
      },
      {
          "name": "assigneeId",
          "type": "string",
          "label": "Assignee",
          "area": "main"
      },
      {
          "name": "lastDoneById",
          "type": "string",
          "label": "Last Done By",
          "area": "main"
      },
      {
          "name": "lastDoneAt",
          "type": "date",
          "label": "Last Done At",
          "area": "main"
      },
      {
          "name": "notes",
          "type": "text",
          "label": "Notes",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_choresChoresConfig, 'schema', {
  value: choresChoreSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const choresChoresConfig = _choresChoresConfig as typeof _choresChoresConfig & { schema: typeof choresChoreSchema }

export const useChoresChores = () => choresChoresConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: choresChoresConfig.defaultValues,
    schema: choresChoreSchema,
    columns: choresChoresConfig.columns,
    collection: choresChoresConfig.name
  }
}