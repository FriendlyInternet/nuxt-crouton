# LendLoans Collection

> Generated by @fyit/crouton-cli on 2026-08-04

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `lendLoans` |
| Layer | `lend` |
| API Base | `/api/teams/[teamId]/lend-loans` |
| Form Component | `<LendLoansForm />` |
| List Component | `<LendLoansList />` |
| Composable | `useLendLoans()` |

## File Structure

```
collections/loans/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useLendLoans.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/lend-loans/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [loanId].patch.ts    # UPDATE
│   │   └── [loanId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| itemName | string | Yes |
| borrowerName | string | Yes |
| lentDate | date | Yes |
| expectedBackDate | date | No |
| returned | boolean | Yes |
| notes | text | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/lend-loans

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/lend-loans`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/lend-loans?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/lend-loans

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/lend-loans`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/lend-loans/{loanId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/lend-loans/${loanId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/lend-loans/{loanId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/lend-loans/${loanId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: loans, pending } = await useCollectionQuery('lendLoans')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('lendLoans')
</script>

<template>
  <LendLoansList />
</template>
```

### Schema & Validation

```typescript
import { lendLoanSchema } from './app/composables/useLendLoans'

// Validate data
const result = lendLoanSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only loans`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useLendLoans.ts`:
```typescript
export const lendLoanSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/lend-loans/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
