/**
 * @crouton-generated
 * @collection loans
 * @layer lend
 * @generated 2026-08-04
 *
 * ## AI Context
 * - Composable: useLendLoans
 * - Collection name: lendLoans
 * - API endpoint: /api/teams/[id]/lend-loans
 * - Form component: LendLoansForm
 * - List component: LendLoansList
 * - Fields: itemName, borrowerName, lentDate, expectedBackDate, returned, notes
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const lendLoanSchema = z.object({
  itemName: z.string().min(1, 'itemName is required'),
  borrowerName: z.string().min(1, 'borrowerName is required'),
  lentDate: z.coerce.date(),
  expectedBackDate: z.coerce.date().nullish(),
  returned: z.boolean(),
  notes: z.string().optional()
})

export const lendLoansColumns = [
  { accessorKey: 'itemName', header: 'ItemName' },
  { accessorKey: 'borrowerName', header: 'BorrowerName' },
  { accessorKey: 'lentDate', header: 'LentDate' },
  { accessorKey: 'expectedBackDate', header: 'ExpectedBackDate' },
  { accessorKey: 'returned', header: 'Returned' },
  { accessorKey: 'notes', header: 'Notes' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _lendLoansConfig = {
  name: 'lendLoans',
  layer: 'lend',
  apiPath: 'lend-loans',
  componentName: 'LendLoansForm',
  defaultValues: {
    itemName: '',
    borrowerName: '',
    lentDate: null,
    expectedBackDate: null,
    returned: false,
    notes: ''
  },
  columns: lendLoansColumns,
  fields: [
      {
          "name": "itemName",
          "type": "string",
          "label": "ItemName",
          "area": "main"
      },
      {
          "name": "borrowerName",
          "type": "string",
          "label": "BorrowerName",
          "area": "main"
      },
      {
          "name": "lentDate",
          "type": "date",
          "label": "LentDate",
          "area": "main"
      },
      {
          "name": "expectedBackDate",
          "type": "date",
          "label": "ExpectedBackDate",
          "area": "main"
      },
      {
          "name": "returned",
          "type": "boolean",
          "label": "Returned",
          "area": "main"
      },
      {
          "name": "notes",
          "type": "text",
          "label": "Notes",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_lendLoansConfig, 'schema', {
  value: lendLoanSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const lendLoansConfig = _lendLoansConfig as typeof _lendLoansConfig & { schema: typeof lendLoanSchema }

export const useLendLoans = () => lendLoansConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: lendLoansConfig.defaultValues,
    schema: lendLoanSchema,
    columns: lendLoansConfig.columns,
    collection: lendLoansConfig.name
  }
}