<!--
  @crouton-generated
  @collection loans
  @layer lend
  @generated 2026-08-04

  ## AI Context
  - List component for loans collection
  - Collection name: lendLoans
  - API endpoint: /api/teams/[id]/lend-loans
  - Columns composable: useLendLoans()
  - Column fields: itemName, borrowerName, lentDate, expectedBackDate, returned, notes

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="lendLoans"
    :columns="columns"
    :rows="loans || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="LendLoans"
        :collection="'lendLoans'"
        createButton
      />
    </template>
    <template #lentDate-cell="{ row }">
      <CroutonDate :date="row.original.lentDate" />
    </template>
    <template #expectedBackDate-cell="{ row }">
      <CroutonDate :date="row.original.expectedBackDate" />
    </template>
    <template #returned-cell="{ row }">
      <CroutonBoolean :value="row.original.returned" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useLendLoans from '../composables/useLendLoans'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useLendLoans()

const { items: loans, pending } = await useCollectionQuery(
  'lendLoans'
)

</script>