/**
 * @crouton-generated
 * @collection snippets
 * @layer main
 * @generated 2026-07-10
 *
 * ## AI Context
 * - Main interface: MainSnippet
 * - Form data type: MainSnippetFormData
 * - New item type: NewMainSnippet
 * - Form props: MainSnippetFormProps
 * - Fields: title, code, language, tags
 *
 * ## Common Modifications
 * - Add field: Add to interface and ensure schema matches
 * - Change field type: Update both interface and schema
 * - Add computed field: Add optional property to interface
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import type { z } from 'zod'
import type { mainSnippetSchema } from './app/composables/useMainSnippets'

export interface MainSnippet {
  id: string
  teamId: string
  owner: string
  title: string
  code: string
  language?: string
  tags?: string[]
  createdAt: Date
  updatedAt: Date
  createdBy: string
  updatedBy: string
  optimisticId?: string
  optimisticAction?: 'create' | 'update' | 'delete'
}

export type MainSnippetFormData = z.infer<typeof mainSnippetSchema>
// New* allows the server-set fields (id, createdBy, updatedBy) as optional — the
// generated POST endpoint provides them; other callers (e.g. seed) may omit them.
export type NewMainSnippet = Omit<MainSnippet, 'id' | 'createdAt' | 'updatedAt' | 'createdBy' | 'updatedBy'> & { id?: string; createdBy?: string; updatedBy?: string }

// Props type for the Form component
export interface MainSnippetFormProps {
  items: string[] // Array of IDs for delete action
  activeItem: MainSnippet | Record<string, never> // MainSnippet for update, empty object for create
  collection: string
  loading: string
  action: 'create' | 'update' | 'delete'
}