# MainSnippets Collection

> Generated by @fyit/crouton-cli on 2026-07-10

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `mainSnippets` |
| Layer | `main` |
| API Base | `/api/teams/[teamId]/main-snippets` |
| Form Component | `<MainSnippetsForm />` |
| List Component | `<MainSnippetsList />` |
| Composable | `useMainSnippets()` |

## File Structure

```
collections/snippets/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useMainSnippets.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/main-snippets/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [snippetId].patch.ts    # UPDATE
│   │   └── [snippetId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| code | text | Yes |
| language | string | No |
| tags | array | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/main-snippets

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/main-snippets`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/main-snippets?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/main-snippets

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/main-snippets`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/main-snippets/{snippetId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/main-snippets/${snippetId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/main-snippets/{snippetId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/main-snippets/${snippetId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: snippets, pending } = await useCollectionQuery('mainSnippets')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('mainSnippets')
</script>

<template>
  <MainSnippetsList />
</template>
```

### Schema & Validation

```typescript
import { mainSnippetSchema } from './app/composables/useMainSnippets'

// Validate data
const result = mainSnippetSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only snippets`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useMainSnippets.ts`:
```typescript
export const mainSnippetSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/main-snippets/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
