/**
 * @crouton-generated
 * @collection snippets
 * @layer main
 * @generated 2026-07-10
 *
 * ## AI Context
 * - Composable: useMainSnippets
 * - Collection name: mainSnippets
 * - API endpoint: /api/teams/[id]/main-snippets
 * - Form component: MainSnippetsForm
 * - List component: MainSnippetsList
 * - Fields: title, code, language, tags
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const mainSnippetSchema = z.object({
  title: z.string().min(1, 'title is required'),
  code: z.string().min(1, 'code is required'),
  language: z.string().optional(),
  tags: z.array(z.string()).optional()
})

export const mainSnippetsColumns = [
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'code', header: 'Code' },
  { accessorKey: 'language', header: 'Language' },
  { accessorKey: 'tags', header: 'Tags' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _mainSnippetsConfig = {
  name: 'mainSnippets',
  layer: 'main',
  apiPath: 'main-snippets',
  componentName: 'MainSnippetsForm',
  defaultValues: {
    title: '',
    code: '',
    language: '',
    tags: []
  },
  columns: mainSnippetsColumns,
  fields: [
      {
          "name": "title",
          "type": "string",
          "label": "Title",
          "area": "main"
      },
      {
          "name": "code",
          "type": "text",
          "label": "Code",
          "area": "main"
      },
      {
          "name": "language",
          "type": "string",
          "label": "Language",
          "area": "main"
      },
      {
          "name": "tags",
          "type": "array",
          "label": "Tags",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_mainSnippetsConfig, 'schema', {
  value: mainSnippetSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const mainSnippetsConfig = _mainSnippetsConfig as typeof _mainSnippetsConfig & { schema: typeof mainSnippetSchema }

export const useMainSnippets = () => mainSnippetsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: mainSnippetsConfig.defaultValues,
    schema: mainSnippetSchema,
    columns: mainSnippetsConfig.columns,
    collection: mainSnippetsConfig.name
  }
}