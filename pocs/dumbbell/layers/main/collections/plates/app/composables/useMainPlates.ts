/**
 * @crouton-generated
 * @collection plates
 * @layer main
 * @generated 2026-08-05
 *
 * ## AI Context
 * - Composable: useMainPlates
 * - Collection name: mainPlates
 * - API endpoint: /api/teams/[id]/main-plates
 * - Form component: MainPlatesForm
 * - List component: MainPlatesList
 * - Fields: name, weight
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const mainPlateSchema = z.object({
  name: z.string().min(1, 'name is required'),
  weight: z.number()
})

export const mainPlatesColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'weight', header: 'Weight' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _mainPlatesConfig = {
  name: 'mainPlates',
  layer: 'main',
  apiPath: 'main-plates',
  componentName: 'MainPlatesForm',
  defaultValues: {
    name: '',
    weight: 0
  },
  columns: mainPlatesColumns,
  fields: [
      {
          "name": "name",
          "type": "string",
          "label": "Name",
          "area": "main"
      },
      {
          "name": "weight",
          "type": "number",
          "label": "Weight",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_mainPlatesConfig, 'schema', {
  value: mainPlateSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const mainPlatesConfig = _mainPlatesConfig as typeof _mainPlatesConfig & { schema: typeof mainPlateSchema }

export const useMainPlates = () => mainPlatesConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: mainPlatesConfig.defaultValues,
    schema: mainPlateSchema,
    columns: mainPlatesConfig.columns,
    collection: mainPlatesConfig.name
  }
}