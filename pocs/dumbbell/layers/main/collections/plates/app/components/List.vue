<!--
  @crouton-generated
  @collection plates
  @layer main
  @generated 2026-08-05

  ## AI Context
  - List component for plates collection
  - Collection name: mainPlates
  - API endpoint: /api/teams/[id]/main-plates
  - Columns composable: useMainPlates()
  - Column fields: name, weight

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="mainPlates"
    :columns="columns"
    :rows="plates || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="MainPlates"
        :collection="'mainPlates'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useMainPlates from '../composables/useMainPlates'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useMainPlates()

const { items: plates, pending } = await useCollectionQuery(
  'mainPlates'
)

</script>