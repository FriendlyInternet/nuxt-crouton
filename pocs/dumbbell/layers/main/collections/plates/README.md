# MainPlates Collection

> Generated by @fyit/crouton-cli on 2026-08-05

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `mainPlates` |
| Layer | `main` |
| API Base | `/api/teams/[teamId]/main-plates` |
| Form Component | `<MainPlatesForm />` |
| List Component | `<MainPlatesList />` |
| Composable | `useMainPlates()` |

## File Structure

```
collections/plates/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useMainPlates.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/main-plates/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [plateId].patch.ts    # UPDATE
│   │   └── [plateId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| weight | number | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/main-plates

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/main-plates`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/main-plates?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/main-plates

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/main-plates`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/main-plates/{plateId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/main-plates/${plateId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/main-plates/{plateId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/main-plates/${plateId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: plates, pending } = await useCollectionQuery('mainPlates')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('mainPlates')
</script>

<template>
  <MainPlatesList />
</template>
```

### Schema & Validation

```typescript
import { mainPlateSchema } from './app/composables/useMainPlates'

// Validate data
const result = mainPlateSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only plates`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useMainPlates.ts`:
```typescript
export const mainPlateSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/main-plates/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
