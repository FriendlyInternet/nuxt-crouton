/**
 * @crouton-generated
 * @collection pages
 * @layer builder
 * @generated 2026-07-02
 *
 * ## AI Context
 * - Composable: useBuilderPages
 * - Collection name: builderPages
 * - API endpoint: /api/teams/[id]/builder-pages
 * - Form component: BuilderPagesForm
 * - List component: BuilderPagesList
 * - Fields: title, slug, isHome, status, showInNavigation, board
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const builderPageSchema = z.object({
  title: z.string().min(1, 'title is required'),
  slug: z.string().min(1, 'slug is required'),
  isHome: z.boolean().optional(),
  status: z.string().min(1, 'status is required'),
  showInNavigation: z.boolean().optional(),
  board: z.record(z.string(), z.any()).nullish(),
  parentId: z.string().nullable().optional()
})

export const builderPagesColumns = [
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'slug', header: 'Slug' },
  { accessorKey: 'isHome', header: 'IsHome' },
  { accessorKey: 'status', header: 'Status' },
  { accessorKey: 'showInNavigation', header: 'ShowInNavigation' },
  { accessorKey: 'board', header: 'Board' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _builderPagesConfig = {
  name: 'builderPages',
  layer: 'builder',
  apiPath: 'builder-pages',
  componentName: 'CroutonPagesForm',
  defaultValues: {
    title: '',
    slug: '',
    isHome: false,
    status: '',
    showInNavigation: false,
    board: {},
    parentId: null
  },
  columns: builderPagesColumns,
  hierarchy: {
    enabled: true,
    parentField: 'parentId',
    pathField: 'path',
    depthField: 'depth',
    orderField: 'order'
  },
  fields: [
      {
          "name": "title",
          "type": "string",
          "label": "Title",
          "area": "main"
      },
      {
          "name": "slug",
          "type": "string",
          "label": "URL Slug",
          "area": "main"
      },
      {
          "name": "isHome",
          "type": "boolean",
          "label": "Home page",
          "area": "sidebar"
      },
      {
          "name": "status",
          "type": "string",
          "label": "Status",
          "area": "sidebar"
      },
      {
          "name": "showInNavigation",
          "type": "boolean",
          "label": "Show in navigation",
          "area": "sidebar"
      },
      {
          "name": "board",
          "type": "json",
          "label": "Builder board",
          "area": "meta"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_builderPagesConfig, 'schema', {
  value: builderPageSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const builderPagesConfig = _builderPagesConfig as typeof _builderPagesConfig & { schema: typeof builderPageSchema }

export const useBuilderPages = () => builderPagesConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: builderPagesConfig.defaultValues,
    schema: builderPageSchema,
    columns: builderPagesConfig.columns,
    collection: builderPagesConfig.name
  }
}