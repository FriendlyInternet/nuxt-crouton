<!--
  @crouton-generated
  @collection pages
  @layer builder
  @generated 2026-07-02

  ## AI Context
  - List component for pages collection
  - Collection name: builderPages
  - API endpoint: /api/teams/[id]/builder-pages
  - Columns composable: useBuilderPages()
  - Column fields: title, slug, isHome, status, showInNavigation, board

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="builderPages"
    :columns="columns"
    :rows="pages || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="BuilderPages"
        :collection="'builderPages'"
        createButton
      />
    </template>
    <template #isHome-cell="{ row }">
      <CroutonBoolean :value="row.original.isHome" />
    </template>
    <template #showInNavigation-cell="{ row }">
      <CroutonBoolean :value="row.original.showInNavigation" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBuilderPages from '../composables/useBuilderPages'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useBuilderPages()

const { items: pages, pending } = await useCollectionQuery(
  'builderPages'
)

</script>