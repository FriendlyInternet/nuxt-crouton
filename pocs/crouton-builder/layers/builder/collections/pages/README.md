# BuilderPages Collection

> Generated by @fyit/crouton-cli on 2026-07-02

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `builderPages` |
| Layer | `builder` |
| API Base | `/api/teams/[teamId]/builder-pages` |
| Form Component | `<BuilderPagesForm />` |
| List Component | `<BuilderPagesList />` |
| Composable | `useBuilderPages()` |

## Features

- Tree/hierarchy structure with parent-child relationships

## File Structure

```
collections/pages/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBuilderPages.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/builder-pages/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [pageId].patch.ts    # UPDATE
│   │   └── [pageId].delete.ts   # DELETE
│   │   ├── [pageId]/move.patch.ts  # Move in tree
│   │   └── reorder.patch.ts      # Reorder siblings
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| slug | string | Yes |
| isHome | boolean | No |
| status | string | Yes |
| showInNavigation | boolean | No |
| board | json | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/builder-pages

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/builder-pages`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/builder-pages?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/builder-pages

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/builder-pages`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/builder-pages/{pageId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/builder-pages/${pageId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/builder-pages/{pageId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/builder-pages/${pageId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: pages, pending } = await useCollectionQuery('builderPages')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('builderPages')
</script>

<template>
  <BuilderPagesList />
</template>
```

### Schema & Validation

```typescript
import { builderPageSchema } from './app/composables/useBuilderPages'

// Validate data
const result = builderPageSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only pages`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBuilderPages.ts`:
```typescript
export const builderPageSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/builder-pages/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
