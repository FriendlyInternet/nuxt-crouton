/**
 * @crouton-generated
 * @collection artists
 * @layer builder
 * @generated 2026-07-02
 *
 * ## AI Context
 * - Composable: useBuilderArtists
 * - Collection name: builderArtists
 * - API endpoint: /api/teams/[id]/builder-artists
 * - Form component: BuilderArtistsForm
 * - List component: BuilderArtistsList
 * - Fields: name, genre, image, bio, rating, active
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const builderArtistSchema = z.object({
  name: z.string().min(1, 'name is required'),
  genre: z.string().min(1, 'genre is required'),
  image: z.string().optional(),
  bio: z.string().optional(),
  rating: z.number().optional(),
  active: z.boolean().optional()
})

export const builderArtistsColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'genre', header: 'Genre' },
  { accessorKey: 'image', header: 'Image' },
  { accessorKey: 'bio', header: 'Bio' },
  { accessorKey: 'rating', header: 'Rating' },
  { accessorKey: 'active', header: 'Active' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _builderArtistsConfig = {
  name: 'builderArtists',
  layer: 'builder',
  apiPath: 'builder-artists',
  componentName: 'BuilderArtistsForm',
  defaultValues: {
    name: '',
    genre: '',
    image: '',
    bio: '',
    rating: 0,
    active: false
  },
  columns: builderArtistsColumns,
  fields: [
      {
          "name": "name",
          "type": "string",
          "label": "Name",
          "area": "main"
      },
      {
          "name": "genre",
          "type": "string",
          "label": "Genre",
          "area": "main"
      },
      {
          "name": "image",
          "type": "image",
          "label": "Photo",
          "area": "main"
      },
      {
          "name": "bio",
          "type": "text",
          "label": "Bio",
          "area": "main"
      },
      {
          "name": "rating",
          "type": "number",
          "label": "Rating",
          "area": "sidebar"
      },
      {
          "name": "active",
          "type": "boolean",
          "label": "Active",
          "area": "sidebar"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_builderArtistsConfig, 'schema', {
  value: builderArtistSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const builderArtistsConfig = _builderArtistsConfig as typeof _builderArtistsConfig & { schema: typeof builderArtistSchema }

export const useBuilderArtists = () => builderArtistsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: builderArtistsConfig.defaultValues,
    schema: builderArtistSchema,
    columns: builderArtistsConfig.columns,
    collection: builderArtistsConfig.name
  }
}