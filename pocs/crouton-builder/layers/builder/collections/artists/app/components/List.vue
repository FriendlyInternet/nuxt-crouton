<!--
  @crouton-generated
  @collection artists
  @layer builder
  @generated 2026-07-02

  ## AI Context
  - List component for artists collection
  - Collection name: builderArtists
  - API endpoint: /api/teams/[id]/builder-artists
  - Columns composable: useBuilderArtists()
  - Column fields: name, genre, image, bio, rating, active

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="builderArtists"
    :columns="columns"
    :rows="artists || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="BuilderArtists"
        :collection="'builderArtists'"
        createButton
      />
    </template>
    <template #active-cell="{ row }">
      <CroutonBoolean :value="row.original.active" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBuilderArtists from '../composables/useBuilderArtists'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useBuilderArtists()

const { items: artists, pending } = await useCollectionQuery(
  'builderArtists'
)

</script>