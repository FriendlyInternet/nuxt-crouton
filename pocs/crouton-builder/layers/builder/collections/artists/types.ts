/**
 * @crouton-generated
 * @collection artists
 * @layer builder
 * @generated 2026-07-02
 *
 * ## AI Context
 * - Main interface: BuilderArtist
 * - Form data type: BuilderArtistFormData
 * - New item type: NewBuilderArtist
 * - Form props: BuilderArtistFormProps
 * - Fields: name, genre, image, bio, rating, active
 *
 * ## Common Modifications
 * - Add field: Add to interface and ensure schema matches
 * - Change field type: Update both interface and schema
 * - Add computed field: Add optional property to interface
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import type { z } from 'zod'
import type { builderArtistSchema } from './app/composables/useBuilderArtists'

export interface BuilderArtist {
  id: string
  teamId: string
  owner: string
  name: string
  genre: string
  image?: string
  bio?: string
  rating?: number
  active?: boolean
  createdAt: Date
  updatedAt: Date
  createdBy: string
  updatedBy: string
  optimisticId?: string
  optimisticAction?: 'create' | 'update' | 'delete'
}

export type BuilderArtistFormData = z.infer<typeof builderArtistSchema>
// New* allows the server-set fields (id, createdBy, updatedBy) as optional — the
// generated POST endpoint provides them; other callers (e.g. seed) may omit them.
export type NewBuilderArtist = Omit<BuilderArtist, 'id' | 'createdAt' | 'updatedAt' | 'createdBy' | 'updatedBy'> & { id?: string; createdBy?: string; updatedBy?: string }

// Props type for the Form component
export interface BuilderArtistFormProps {
  items: string[] // Array of IDs for delete action
  activeItem: BuilderArtist | Record<string, never> // BuilderArtist for update, empty object for create
  collection: string
  loading: string
  action: 'create' | 'update' | 'delete'
}