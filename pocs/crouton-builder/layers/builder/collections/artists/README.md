# BuilderArtists Collection

> Generated by @fyit/crouton-cli on 2026-07-02

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `builderArtists` |
| Layer | `builder` |
| API Base | `/api/teams/[teamId]/builder-artists` |
| Form Component | `<BuilderArtistsForm />` |
| List Component | `<BuilderArtistsList />` |
| Composable | `useBuilderArtists()` |

## File Structure

```
collections/artists/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useBuilderArtists.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/builder-artists/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [artistId].patch.ts    # UPDATE
│   │   └── [artistId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| genre | string | Yes |
| image | image | No |
| bio | text | No |
| rating | number | No |
| active | boolean | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/builder-artists

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/builder-artists`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/builder-artists?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/builder-artists

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/builder-artists`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/builder-artists/{artistId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/builder-artists/${artistId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/builder-artists/{artistId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/builder-artists/${artistId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: artists, pending } = await useCollectionQuery('builderArtists')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('builderArtists')
</script>

<template>
  <BuilderArtistsList />
</template>
```

### Schema & Validation

```typescript
import { builderArtistSchema } from './app/composables/useBuilderArtists'

// Validate data
const result = builderArtistSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only artists`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useBuilderArtists.ts`:
```typescript
export const builderArtistSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/builder-artists/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
