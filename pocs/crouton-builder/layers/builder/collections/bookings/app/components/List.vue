<!--
  @crouton-generated
  @collection bookings
  @layer builder
  @generated 2026-07-02

  ## AI Context
  - List component for bookings collection
  - Collection name: builderBookings
  - API endpoint: /api/teams/[id]/builder-bookings
  - Columns composable: useBuilderBookings()
  - Column fields: artist, venue, date, fee, status

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="builderBookings"
    :columns="columns"
    :rows="bookings || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="BuilderBookings"
        :collection="'builderBookings'"
        createButton
      />
    </template>
    <template #artist-cell="{ row }">
      <CroutonItemCardMini
        v-if="row.original.artist"
        :id="row.original.artist"
        collection="builderArtists"
      />
    </template>
    <template #date-cell="{ row }">
      <CroutonDate :date="row.original.date"></CroutonDate>
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useBuilderBookings from '../composables/useBuilderBookings'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useBuilderBookings()

const { items: bookings, pending } = await useCollectionQuery(
  'builderBookings'
)

</script>