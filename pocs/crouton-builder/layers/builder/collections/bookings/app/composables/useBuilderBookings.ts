/**
 * @crouton-generated
 * @collection bookings
 * @layer builder
 * @generated 2026-07-02
 *
 * ## AI Context
 * - Composable: useBuilderBookings
 * - Collection name: builderBookings
 * - API endpoint: /api/teams/[id]/builder-bookings
 * - Form component: BuilderBookingsForm
 * - List component: BuilderBookingsList
 * - Fields: artist, venue, date, fee, status
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const builderBookingSchema = z.object({
  artist: z.string().min(1, 'artist is required'),
  venue: z.string().min(1, 'venue is required'),
  date: z.coerce.date(),
  fee: z.number().optional(),
  status: z.string().min(1, 'status is required')
})

export const builderBookingsColumns = [
  { accessorKey: 'artist', header: 'Artist' },
  { accessorKey: 'venue', header: 'Venue' },
  { accessorKey: 'date', header: 'Date' },
  { accessorKey: 'fee', header: 'Fee' },
  { accessorKey: 'status', header: 'Status' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _builderBookingsConfig = {
  name: 'builderBookings',
  layer: 'builder',
  apiPath: 'builder-bookings',
  componentName: 'BuilderBookingsForm',
  defaultValues: {
    artist: '',
    venue: '',
    date: null,
    fee: 0,
    status: ''
  },
  columns: builderBookingsColumns,
  fields: [
      {
          "name": "artist",
          "type": "string",
          "label": "Artist",
          "area": "main"
      },
      {
          "name": "venue",
          "type": "string",
          "label": "Venue",
          "area": "main"
      },
      {
          "name": "date",
          "type": "date",
          "label": "Date",
          "area": "main"
      },
      {
          "name": "fee",
          "type": "decimal",
          "label": "Fee",
          "area": "sidebar"
      },
      {
          "name": "status",
          "type": "string",
          "label": "Status",
          "area": "sidebar"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_builderBookingsConfig, 'schema', {
  value: builderBookingSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const builderBookingsConfig = _builderBookingsConfig as typeof _builderBookingsConfig & { schema: typeof builderBookingSchema }

export const useBuilderBookings = () => builderBookingsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: builderBookingsConfig.defaultValues,
    schema: builderBookingSchema,
    columns: builderBookingsConfig.columns,
    collection: builderBookingsConfig.name
  }
}