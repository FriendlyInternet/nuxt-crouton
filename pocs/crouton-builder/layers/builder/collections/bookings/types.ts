/**
 * @crouton-generated
 * @collection bookings
 * @layer builder
 * @generated 2026-07-02
 *
 * ## AI Context
 * - Main interface: BuilderBooking
 * - Form data type: BuilderBookingFormData
 * - New item type: NewBuilderBooking
 * - Form props: BuilderBookingFormProps
 * - Fields: artist, venue, date, fee, status
 *
 * ## Common Modifications
 * - Add field: Add to interface and ensure schema matches
 * - Change field type: Update both interface and schema
 * - Add computed field: Add optional property to interface
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import type { z } from 'zod'
import type { builderBookingSchema } from './app/composables/useBuilderBookings'

export interface BuilderBooking {
  id: string
  teamId: string
  owner: string
  artist: string
  venue: string
  date: Date
  fee?: number
  status: string
  createdAt: Date
  updatedAt: Date
  createdBy: string
  updatedBy: string
  optimisticId?: string
  optimisticAction?: 'create' | 'update' | 'delete'
}

export type BuilderBookingFormData = z.infer<typeof builderBookingSchema>
// New* allows the server-set fields (id, createdBy, updatedBy) as optional — the
// generated POST endpoint provides them; other callers (e.g. seed) may omit them.
export type NewBuilderBooking = Omit<BuilderBooking, 'id' | 'createdAt' | 'updatedAt' | 'createdBy' | 'updatedBy'> & { id?: string; createdBy?: string; updatedBy?: string }

// Props type for the Form component
export interface BuilderBookingFormProps {
  items: string[] // Array of IDs for delete action
  activeItem: BuilderBooking | Record<string, never> // BuilderBooking for update, empty object for create
  collection: string
  loading?: string // optional: the form reads useCrouton().loading; rendered as a bare layout block it isn't passed
  action: 'create' | 'update' | 'delete'
}