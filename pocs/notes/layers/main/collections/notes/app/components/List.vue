<!--
  @crouton-generated
  @collection notes
  @layer main
  @generated 2026-07-08

  ## AI Context
  - List component for notes collection
  - Collection name: mainNotes
  - API endpoint: /api/teams/[id]/main-notes
  - Columns composable: useMainNotes()
  - Column fields: title, body, tags

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="mainNotes"
    :columns="columns"
    :rows="notes || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="MainNotes"
        :collection="'mainNotes'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useMainNotes from '../composables/useMainNotes'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useMainNotes()

const { items: notes, pending } = await useCollectionQuery(
  'mainNotes'
)

</script>