/**
 * @crouton-generated
 * @collection notes
 * @layer main
 * @generated 2026-07-08
 *
 * ## AI Context
 * - Composable: useMainNotes
 * - Collection name: mainNotes
 * - API endpoint: /api/teams/[id]/main-notes
 * - Form component: MainNotesForm
 * - List component: MainNotesList
 * - Fields: title, body, tags
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const mainNoteSchema = z.object({
  title: z.string().min(1, 'title is required'),
  body: z.string().optional(),
  tags: z.string().optional()
})

export const mainNotesColumns = [
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'body', header: 'Body' },
  { accessorKey: 'tags', header: 'Tags' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _mainNotesConfig = {
  name: 'mainNotes',
  layer: 'main',
  apiPath: 'main-notes',
  componentName: 'MainNotesForm',
  defaultValues: {
    title: '',
    body: '',
    tags: ''
  },
  columns: mainNotesColumns,
  fields: [
      {
          "name": "title",
          "type": "string",
          "label": "Title",
          "area": "main"
      },
      {
          "name": "body",
          "type": "text",
          "label": "Body",
          "area": "main"
      },
      {
          "name": "tags",
          "type": "string",
          "label": "Tags",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_mainNotesConfig, 'schema', {
  value: mainNoteSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const mainNotesConfig = _mainNotesConfig as typeof _mainNotesConfig & { schema: typeof mainNoteSchema }

export const useMainNotes = () => mainNotesConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: mainNotesConfig.defaultValues,
    schema: mainNoteSchema,
    columns: mainNotesConfig.columns,
    collection: mainNotesConfig.name
  }
}