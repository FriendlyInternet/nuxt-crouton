# MainQuotes Collection

> Generated by @fyit/crouton-cli on 2026-07-09

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `mainQuotes` |
| Layer | `main` |
| API Base | `/api/teams/[teamId]/main-quotes` |
| Form Component | `<MainQuotesForm />` |
| List Component | `<MainQuotesList />` |
| Composable | `useMainQuotes()` |

## File Structure

```
collections/quotes/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useMainQuotes.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/main-quotes/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [quoteId].patch.ts    # UPDATE
│   │   └── [quoteId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| text | text | Yes |
| author | string | No |
| source | string | No |
| tags | array | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/main-quotes

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/main-quotes`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/main-quotes?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/main-quotes

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/main-quotes`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/main-quotes/{quoteId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/main-quotes/${quoteId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/main-quotes/{quoteId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/main-quotes/${quoteId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: quotes, pending } = await useCollectionQuery('mainQuotes')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('mainQuotes')
</script>

<template>
  <MainQuotesList />
</template>
```

### Schema & Validation

```typescript
import { mainQuoteSchema } from './app/composables/useMainQuotes'

// Validate data
const result = mainQuoteSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only quotes`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useMainQuotes.ts`:
```typescript
export const mainQuoteSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/main-quotes/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
