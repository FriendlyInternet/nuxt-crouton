/**
 * @crouton-generated
 * @collection quotes
 * @layer main
 * @generated 2026-07-09
 *
 * ## AI Context
 * - Composable: useMainQuotes
 * - Collection name: mainQuotes
 * - API endpoint: /api/teams/[id]/main-quotes
 * - Form component: MainQuotesForm
 * - List component: MainQuotesList
 * - Fields: text, author, source, tags
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const mainQuoteSchema = z.object({
  text: z.string().min(1, 'text is required'),
  author: z.string().optional(),
  source: z.string().optional(),
  tags: z.array(z.string()).optional()
})

export const mainQuotesColumns = [
  { accessorKey: 'text', header: 'Text' },
  { accessorKey: 'author', header: 'Author' },
  { accessorKey: 'source', header: 'Source' },
  { accessorKey: 'tags', header: 'Tags' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _mainQuotesConfig = {
  name: 'mainQuotes',
  layer: 'main',
  apiPath: 'main-quotes',
  componentName: 'MainQuotesForm',
  defaultValues: {
    text: '',
    author: '',
    source: '',
    tags: []
  },
  columns: mainQuotesColumns,
  fields: [
      {
          "name": "text",
          "type": "text",
          "label": "Text",
          "area": "main"
      },
      {
          "name": "author",
          "type": "string",
          "label": "Author",
          "area": "main"
      },
      {
          "name": "source",
          "type": "string",
          "label": "Source",
          "area": "main"
      },
      {
          "name": "tags",
          "type": "array",
          "label": "Tags",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_mainQuotesConfig, 'schema', {
  value: mainQuoteSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const mainQuotesConfig = _mainQuotesConfig as typeof _mainQuotesConfig & { schema: typeof mainQuoteSchema }

export const useMainQuotes = () => mainQuotesConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: mainQuotesConfig.defaultValues,
    schema: mainQuoteSchema,
    columns: mainQuotesConfig.columns,
    collection: mainQuotesConfig.name
  }
}