<!--
  @crouton-generated
  @collection quotes
  @layer main
  @generated 2026-07-09

  ## AI Context
  - List component for quotes collection
  - Collection name: mainQuotes
  - API endpoint: /api/teams/[id]/main-quotes
  - Columns composable: useMainQuotes()
  - Column fields: text, author, source, tags

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="mainQuotes"
    :columns="columns"
    :rows="quotes || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="MainQuotes"
        :collection="'mainQuotes'"
        createButton
      />
    </template>
    <template #tags-cell="{ row }">
      <div v-if="row.original.tags && row.original.tags.length > 0" class="flex flex-wrap gap-1">
        <UBadge
          v-for="(item, idx) in row.original.tags"
          :key="idx"
          color="neutral"
          variant="subtle"
          size="md"
        >
          {{ item }}
        </UBadge>
      </div>
      <span v-else class="text-gray-400">—</span>
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useMainQuotes from '../composables/useMainQuotes'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useMainQuotes()

const { items: quotes, pending } = await useCollectionQuery(
  'mainQuotes'
)

</script>