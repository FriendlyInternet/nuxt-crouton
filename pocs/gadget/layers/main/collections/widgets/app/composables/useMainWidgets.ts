/**
 * @crouton-generated
 * @collection widgets
 * @layer main
 * @generated 2026-08-04
 *
 * ## AI Context
 * - Composable: useMainWidgets
 * - Collection name: mainWidgets
 * - API endpoint: /api/teams/[id]/main-widgets
 * - Form component: MainWidgetsForm
 * - List component: MainWidgetsList
 * - Fields: name, quantity, active, notes
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const mainWidgetSchema = z.object({
  name: z.string().min(1, 'name is required'),
  quantity: z.number(),
  active: z.boolean(),
  notes: z.string().optional()
})

export const mainWidgetsColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'quantity', header: 'Quantity' },
  { accessorKey: 'active', header: 'Active' },
  { accessorKey: 'notes', header: 'Notes' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _mainWidgetsConfig = {
  name: 'mainWidgets',
  layer: 'main',
  apiPath: 'main-widgets',
  componentName: 'MainWidgetsForm',
  defaultValues: {
    name: '',
    quantity: 0,
    active: false,
    notes: ''
  },
  columns: mainWidgetsColumns,
  fields: [
      {
          "name": "name",
          "type": "string",
          "label": "Name",
          "area": "main"
      },
      {
          "name": "quantity",
          "type": "number",
          "label": "Quantity",
          "area": "main"
      },
      {
          "name": "active",
          "type": "boolean",
          "label": "Active",
          "area": "main"
      },
      {
          "name": "notes",
          "type": "text",
          "label": "Notes",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_mainWidgetsConfig, 'schema', {
  value: mainWidgetSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const mainWidgetsConfig = _mainWidgetsConfig as typeof _mainWidgetsConfig & { schema: typeof mainWidgetSchema }

export const useMainWidgets = () => mainWidgetsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: mainWidgetsConfig.defaultValues,
    schema: mainWidgetSchema,
    columns: mainWidgetsConfig.columns,
    collection: mainWidgetsConfig.name
  }
}