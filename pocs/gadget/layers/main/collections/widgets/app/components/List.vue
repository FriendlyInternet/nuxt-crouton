<!--
  @crouton-generated
  @collection widgets
  @layer main
  @generated 2026-08-04

  ## AI Context
  - List component for widgets collection
  - Collection name: mainWidgets
  - API endpoint: /api/teams/[id]/main-widgets
  - Columns composable: useMainWidgets()
  - Column fields: name, quantity, active, notes

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="mainWidgets"
    :columns="columns"
    :rows="widgets || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="MainWidgets"
        :collection="'mainWidgets'"
        createButton
      />
    </template>
    <template #active-cell="{ row }">
      <CroutonBoolean :value="row.original.active" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useMainWidgets from '../composables/useMainWidgets'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useMainWidgets()

const { items: widgets, pending } = await useCollectionQuery(
  'mainWidgets'
)

</script>