# MainWidgets Collection

> Generated by @fyit/crouton-cli on 2026-08-04

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `mainWidgets` |
| Layer | `main` |
| API Base | `/api/teams/[teamId]/main-widgets` |
| Form Component | `<MainWidgetsForm />` |
| List Component | `<MainWidgetsList />` |
| Composable | `useMainWidgets()` |

## File Structure

```
collections/widgets/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useMainWidgets.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/main-widgets/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [widgetId].patch.ts    # UPDATE
│   │   └── [widgetId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| quantity | number | Yes |
| active | boolean | Yes |
| notes | text | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/main-widgets

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/main-widgets`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/main-widgets?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/main-widgets

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/main-widgets`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/main-widgets/{widgetId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/main-widgets/${widgetId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/main-widgets/{widgetId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/main-widgets/${widgetId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: widgets, pending } = await useCollectionQuery('mainWidgets')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('mainWidgets')
</script>

<template>
  <MainWidgetsList />
</template>
```

### Schema & Validation

```typescript
import { mainWidgetSchema } from './app/composables/useMainWidgets'

// Validate data
const result = mainWidgetSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only widgets`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useMainWidgets.ts`:
```typescript
export const mainWidgetSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/main-widgets/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
