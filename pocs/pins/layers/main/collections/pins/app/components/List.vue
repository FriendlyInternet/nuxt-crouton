<!--
  @crouton-generated
  @collection pins
  @layer main
  @generated 2026-07-03

  ## AI Context
  - List component for pins collection
  - Collection name: mainPins
  - API endpoint: /api/teams/[id]/main-pins
  - Columns composable: useMainPins()
  - Column fields: title, url, note

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="mainPins"
    :columns="columns"
    :rows="pins || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="MainPins"
        :collection="'mainPins'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useMainPins from '../composables/useMainPins'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useMainPins()

const { items: pins, pending } = await useCollectionQuery(
  'mainPins'
)

</script>