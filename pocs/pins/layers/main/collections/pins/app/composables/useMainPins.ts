/**
 * @crouton-generated
 * @collection pins
 * @layer main
 * @generated 2026-07-03
 *
 * ## AI Context
 * - Composable: useMainPins
 * - Collection name: mainPins
 * - API endpoint: /api/teams/[id]/main-pins
 * - Form component: MainPinsForm
 * - List component: MainPinsList
 * - Fields: title, url, note
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const mainPinSchema = z.object({
  title: z.string().min(1, 'title is required'),
  url: z.string().min(1, 'url is required'),
  note: z.string().optional()
})

export const mainPinsColumns = [
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'url', header: 'Url' },
  { accessorKey: 'note', header: 'Note' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _mainPinsConfig = {
  name: 'mainPins',
  layer: 'main',
  apiPath: 'main-pins',
  componentName: 'MainPinsForm',
  defaultValues: {
    title: '',
    url: '',
    note: ''
  },
  columns: mainPinsColumns,
  fields: [
      {
          "name": "title",
          "type": "string",
          "label": "Title",
          "area": "main"
      },
      {
          "name": "url",
          "type": "string",
          "label": "Url",
          "area": "main"
      },
      {
          "name": "note",
          "type": "text",
          "label": "Note",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_mainPinsConfig, 'schema', {
  value: mainPinSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const mainPinsConfig = _mainPinsConfig as typeof _mainPinsConfig & { schema: typeof mainPinSchema }

export const useMainPins = () => mainPinsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: mainPinsConfig.defaultValues,
    schema: mainPinSchema,
    columns: mainPinsConfig.columns,
    collection: mainPinsConfig.name
  }
}