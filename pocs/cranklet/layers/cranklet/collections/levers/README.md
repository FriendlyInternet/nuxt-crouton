# CrankletLevers Collection

> Generated by @fyit/crouton-cli on 2026-08-05

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `crankletLevers` |
| Layer | `cranklet` |
| API Base | `/api/teams/[teamId]/cranklet-levers` |
| Form Component | `<CrankletLeversForm />` |
| List Component | `<CrankletLeversList />` |
| Composable | `useCrankletLevers()` |

## File Structure

```
collections/levers/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useCrankletLevers.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/cranklet-levers/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [leverId].patch.ts    # UPDATE
│   │   └── [leverId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| throw | number | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/cranklet-levers

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/cranklet-levers`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/cranklet-levers?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/cranklet-levers

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/cranklet-levers`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/cranklet-levers/{leverId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/cranklet-levers/${leverId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/cranklet-levers/{leverId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/cranklet-levers/${leverId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: levers, pending } = await useCollectionQuery('crankletLevers')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('crankletLevers')
</script>

<template>
  <CrankletLeversList />
</template>
```

### Schema & Validation

```typescript
import { crankletLeverSchema } from './app/composables/useCrankletLevers'

// Validate data
const result = crankletLeverSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only levers`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useCrankletLevers.ts`:
```typescript
export const crankletLeverSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/cranklet-levers/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
