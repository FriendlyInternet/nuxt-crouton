/**
 * @crouton-generated
 * @collection levers
 * @layer cranklet
 * @generated 2026-08-05
 *
 * ## AI Context
 * - Composable: useCrankletLevers
 * - Collection name: crankletLevers
 * - API endpoint: /api/teams/[id]/cranklet-levers
 * - Form component: CrankletLeversForm
 * - List component: CrankletLeversList
 * - Fields: name, throw
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const crankletLeverSchema = z.object({
  name: z.string().min(1, 'name is required'),
  throw: z.number()
})

export const crankletLeversColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'throw', header: 'Throw' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _crankletLeversConfig = {
  name: 'crankletLevers',
  layer: 'cranklet',
  apiPath: 'cranklet-levers',
  componentName: 'CrankletLeversForm',
  defaultValues: {
    name: '',
    throw: 0
  },
  columns: crankletLeversColumns,
  fields: [
      {
          "name": "name",
          "type": "string",
          "label": "Name",
          "area": "main"
      },
      {
          "name": "throw",
          "type": "number",
          "label": "Throw",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_crankletLeversConfig, 'schema', {
  value: crankletLeverSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const crankletLeversConfig = _crankletLeversConfig as typeof _crankletLeversConfig & { schema: typeof crankletLeverSchema }

export const useCrankletLevers = () => crankletLeversConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: crankletLeversConfig.defaultValues,
    schema: crankletLeverSchema,
    columns: crankletLeversConfig.columns,
    collection: crankletLeversConfig.name
  }
}