<!--
  @crouton-generated
  @collection levers
  @layer cranklet
  @generated 2026-08-05

  ## AI Context
  - List component for levers collection
  - Collection name: crankletLevers
  - API endpoint: /api/teams/[id]/cranklet-levers
  - Columns composable: useCrankletLevers()
  - Column fields: name, throw

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="crankletLevers"
    :columns="columns"
    :rows="levers || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="CrankletLevers"
        :collection="'crankletLevers'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useCrankletLevers from '../composables/useCrankletLevers'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useCrankletLevers()

const { items: levers, pending } = await useCollectionQuery(
  'crankletLevers'
)

</script>