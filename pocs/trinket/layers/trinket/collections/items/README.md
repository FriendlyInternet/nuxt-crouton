# TrinketItems Collection

> Generated by @fyit/crouton-cli on 2026-08-04

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `trinketItems` |
| Layer | `trinket` |
| API Base | `/api/teams/[teamId]/trinket-items` |
| Form Component | `<TrinketItemsForm />` |
| List Component | `<TrinketItemsList />` |
| Composable | `useTrinketItems()` |

## File Structure

```
collections/items/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useTrinketItems.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/trinket-items/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [itemId].patch.ts    # UPDATE
│   │   └── [itemId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| label | string | No |
| count | number | No |
| done | boolean | No |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/trinket-items

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/trinket-items`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/trinket-items?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/trinket-items

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/trinket-items`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/trinket-items/{itemId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/trinket-items/${itemId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/trinket-items/{itemId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/trinket-items/${itemId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: items, pending } = await useCollectionQuery('trinketItems')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('trinketItems')
</script>

<template>
  <TrinketItemsList />
</template>
```

### Schema & Validation

```typescript
import { trinketItemSchema } from './app/composables/useTrinketItems'

// Validate data
const result = trinketItemSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only items`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useTrinketItems.ts`:
```typescript
export const trinketItemSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/trinket-items/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
