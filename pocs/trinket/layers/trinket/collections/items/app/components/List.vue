<!--
  @crouton-generated
  @collection items
  @layer trinket
  @generated 2026-08-04

  ## AI Context
  - List component for items collection
  - Collection name: trinketItems
  - API endpoint: /api/teams/[id]/trinket-items
  - Columns composable: useTrinketItems()
  - Column fields: label, count, done

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="trinketItems"
    :columns="columns"
    :rows="items || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="TrinketItems"
        :collection="'trinketItems'"
        createButton
      />
    </template>
    <template #done-cell="{ row }">
      <CroutonBoolean :value="row.original.done" />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useTrinketItems from '../composables/useTrinketItems'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useTrinketItems()

const { items: items, pending } = await useCollectionQuery(
  'trinketItems'
)

</script>