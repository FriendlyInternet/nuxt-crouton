/**
 * @crouton-generated
 * @collection items
 * @layer trinket
 * @generated 2026-08-04
 *
 * ## AI Context
 * - Composable: useTrinketItems
 * - Collection name: trinketItems
 * - API endpoint: /api/teams/[id]/trinket-items
 * - Form component: TrinketItemsForm
 * - List component: TrinketItemsList
 * - Fields: label, count, done
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const trinketItemSchema = z.object({
  label: z.string().optional(),
  count: z.number().optional(),
  done: z.boolean().optional()
})

export const trinketItemsColumns = [
  { accessorKey: 'label', header: 'Label' },
  { accessorKey: 'count', header: 'Count' },
  { accessorKey: 'done', header: 'Done' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _trinketItemsConfig = {
  name: 'trinketItems',
  layer: 'trinket',
  apiPath: 'trinket-items',
  componentName: 'TrinketItemsForm',
  defaultValues: {
    label: '',
    count: 0,
    done: false
  },
  columns: trinketItemsColumns,
  fields: [
      {
          "name": "label",
          "type": "string",
          "label": "Label",
          "area": "main"
      },
      {
          "name": "count",
          "type": "number",
          "label": "Count",
          "area": "main"
      },
      {
          "name": "done",
          "type": "boolean",
          "label": "Done",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_trinketItemsConfig, 'schema', {
  value: trinketItemSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const trinketItemsConfig = _trinketItemsConfig as typeof _trinketItemsConfig & { schema: typeof trinketItemSchema }

export const useTrinketItems = () => trinketItemsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: trinketItemsConfig.defaultValues,
    schema: trinketItemSchema,
    columns: trinketItemsConfig.columns,
    collection: trinketItemsConfig.name
  }
}