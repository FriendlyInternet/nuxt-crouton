<!--
  @crouton-generated
  @collection parts
  @layer widget
  @generated 2026-08-04

  ## AI Context
  - List component for parts collection
  - Collection name: widgetParts
  - API endpoint: /api/teams/[id]/widget-parts
  - Columns composable: useWidgetParts()
  - Column fields: title, qty

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="widgetParts"
    :columns="columns"
    :rows="parts || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="WidgetParts"
        :collection="'widgetParts'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useWidgetParts from '../composables/useWidgetParts'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useWidgetParts()

const { items: parts, pending } = await useCollectionQuery(
  'widgetParts'
)

</script>