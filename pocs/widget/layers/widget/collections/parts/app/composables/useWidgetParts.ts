/**
 * @crouton-generated
 * @collection parts
 * @layer widget
 * @generated 2026-08-04
 *
 * ## AI Context
 * - Composable: useWidgetParts
 * - Collection name: widgetParts
 * - API endpoint: /api/teams/[id]/widget-parts
 * - Form component: WidgetPartsForm
 * - List component: WidgetPartsList
 * - Fields: title, qty
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const widgetPartSchema = z.object({
  title: z.string().min(1, 'title is required'),
  qty: z.number()
})

export const widgetPartsColumns = [
  { accessorKey: 'title', header: 'Title' },
  { accessorKey: 'qty', header: 'Qty' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _widgetPartsConfig = {
  name: 'widgetParts',
  layer: 'widget',
  apiPath: 'widget-parts',
  componentName: 'WidgetPartsForm',
  defaultValues: {
    title: '',
    qty: 0
  },
  columns: widgetPartsColumns,
  fields: [
      {
          "name": "title",
          "type": "string",
          "label": "Title",
          "area": "main"
      },
      {
          "name": "qty",
          "type": "number",
          "label": "Quantity",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_widgetPartsConfig, 'schema', {
  value: widgetPartSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const widgetPartsConfig = _widgetPartsConfig as typeof _widgetPartsConfig & { schema: typeof widgetPartSchema }

export const useWidgetParts = () => widgetPartsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: widgetPartsConfig.defaultValues,
    schema: widgetPartSchema,
    columns: widgetPartsConfig.columns,
    collection: widgetPartsConfig.name
  }
}