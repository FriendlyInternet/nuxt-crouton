# WidgetParts Collection

> Generated by @fyit/crouton-cli on 2026-08-04

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `widgetParts` |
| Layer | `widget` |
| API Base | `/api/teams/[teamId]/widget-parts` |
| Form Component | `<WidgetPartsForm />` |
| List Component | `<WidgetPartsList />` |
| Composable | `useWidgetParts()` |

## File Structure

```
collections/parts/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useWidgetParts.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/widget-parts/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [partId].patch.ts    # UPDATE
│   │   └── [partId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| title | string | Yes |
| qty | number | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/widget-parts

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/widget-parts`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/widget-parts?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/widget-parts

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/widget-parts`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/widget-parts/{partId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/widget-parts/${partId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/widget-parts/{partId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/widget-parts/${partId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: parts, pending } = await useCollectionQuery('widgetParts')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('widgetParts')
</script>

<template>
  <WidgetPartsList />
</template>
```

### Schema & Validation

```typescript
import { widgetPartSchema } from './app/composables/useWidgetParts'

// Validate data
const result = widgetPartSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only parts`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useWidgetParts.ts`:
```typescript
export const widgetPartSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/widget-parts/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
