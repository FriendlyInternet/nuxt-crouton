/**
 * @crouton-generated
 * @collection cogs
 * @layer sprocket
 * @generated 2026-08-05
 *
 * ## AI Context
 * - Composable: useSprocketCogs
 * - Collection name: sprocketCogs
 * - API endpoint: /api/teams/[id]/sprocket-cogs
 * - Form component: SprocketCogsForm
 * - List component: SprocketCogsList
 * - Fields: name, teeth
 *
 * ## Common Modifications
 * - Add field: Add to schema object and defaultValues
 * - Change validation: Modify z.object() schema
 * - Add column: Add to columns array
 * - Change API path: Modify apiPath in config
 *
 * This file was generated by @fyit/crouton-cli
 * Safe to modify - regeneration requires --force flag
 */

import { z } from 'zod'

// Schema exported separately - Zod 4 schemas cannot survive deep cloning
// Keep schema outside of objects that might be serialized/cloned during SSR
export const sprocketCogSchema = z.object({
  name: z.string().min(1, 'name is required'),
  teeth: z.number()
})

export const sprocketCogsColumns = [
  { accessorKey: 'name', header: 'Name' },
  { accessorKey: 'teeth', header: 'Teeth' }
]

// Config object WITHOUT schema - safe for SSR serialization
const _sprocketCogsConfig = {
  name: 'sprocketCogs',
  layer: 'sprocket',
  apiPath: 'sprocket-cogs',
  componentName: 'SprocketCogsForm',
  defaultValues: {
    name: '',
    teeth: 0
  },
  columns: sprocketCogsColumns,
  fields: [
      {
          "name": "name",
          "type": "string",
          "label": "Name",
          "area": "main"
      },
      {
          "name": "teeth",
          "type": "number",
          "label": "Teeth",
          "area": "main"
      }
  ],
}

// Add schema as non-enumerable property so klona skips it during cloning
Object.defineProperty(_sprocketCogsConfig, 'schema', {
  value: sprocketCogSchema,
  enumerable: false,
  configurable: false,
  writable: false
})

export const sprocketCogsConfig = _sprocketCogsConfig as typeof _sprocketCogsConfig & { schema: typeof sprocketCogSchema }

export const useSprocketCogs = () => sprocketCogsConfig

// Default export for auto-import compatibility
export default function () {
  return {
    defaultValue: sprocketCogsConfig.defaultValues,
    schema: sprocketCogSchema,
    columns: sprocketCogsConfig.columns,
    collection: sprocketCogsConfig.name
  }
}