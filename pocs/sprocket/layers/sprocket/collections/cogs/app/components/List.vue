<!--
  @crouton-generated
  @collection cogs
  @layer sprocket
  @generated 2026-08-05

  ## AI Context
  - List component for cogs collection
  - Collection name: sprocketCogs
  - API endpoint: /api/teams/[id]/sprocket-cogs
  - Columns composable: useSprocketCogs()
  - Column fields: name, teeth

  ## Common Modifications
  - Add column: Update columns array in composable
  - Custom cell template: Add <template #fieldName-cell="{ row }">
  - Change layout: Modify :layout prop (table, list, grid, cards, tree)
  - Add filters: Add query params to useCollectionQuery

  Generated by @fyit/crouton-cli
  Safe to modify - regeneration requires --force flag
-->

<template>
  <CroutonCollection
    :layout="layout"
    collection="sprocketCogs"
    :columns="columns"
    :rows="cogs || []"
    :loading="pending"
  >
    <template #header>
      <CroutonTableHeader
        title="SprocketCogs"
        :collection="'sprocketCogs'"
        createButton
      />
    </template>
  </CroutonCollection>
</template>

<script setup lang="ts">
import useSprocketCogs from '../composables/useSprocketCogs'

const props = withDefaults(defineProps<{
  layout?: any
}>(), {
  layout: 'table'
})

const { columns } = useSprocketCogs()

const { items: cogs, pending } = await useCollectionQuery(
  'sprocketCogs'
)

</script>