# SprocketCogs Collection

> Generated by @fyit/crouton-cli on 2026-08-05

## Overview

| Property | Value |
|----------|-------|
| Collection Name | `sprocketCogs` |
| Layer | `sprocket` |
| API Base | `/api/teams/[teamId]/sprocket-cogs` |
| Form Component | `<SprocketCogsForm />` |
| List Component | `<SprocketCogsList />` |
| Composable | `useSprocketCogs()` |

## File Structure

```
collections/cogs/
├── app/
│   ├── components/
│   │   ├── Form.vue              # CRUD form with Zod validation
│   │   └── List.vue              # Data table with actions
│   └── composables/
│       └── useSprocketCogs.ts   # Schema, columns, config
├── server/
│   ├── api/teams/[id]/sprocket-cogs/
│   │   ├── index.get.ts          # GET all / by IDs
│   │   ├── index.post.ts         # CREATE
│   │   ├── [cogId].patch.ts    # UPDATE
│   │   └── [cogId].delete.ts   # DELETE
│   └── database/
│       ├── schema.ts             # Drizzle ORM schema
│       └── queries.ts            # Database query functions
├── types.ts                      # TypeScript interfaces
├── nuxt.config.ts               # Layer configuration
└── README.md                    # This file
```

## Fields

| Field | Type | Required |
|-------|------|----------|
| id | string (primary key) | Auto |
| name | string | Yes |
| teeth | number | Yes |
| createdAt | timestamp | Auto |
| updatedAt | timestamp | Auto |
| createdBy | string | Auto |
| updatedBy | string | Auto |

## API Endpoints

### GET /api/teams/{teamId}/sprocket-cogs

Fetch all items or specific items by IDs.

```typescript
// Fetch all
const items = await $fetch(`/api/teams/${teamId}/sprocket-cogs`)

// Fetch by IDs
const items = await $fetch(`/api/teams/${teamId}/sprocket-cogs?ids=id1,id2,id3`)
```

### POST /api/teams/{teamId}/sprocket-cogs

Create a new item.

```typescript
const newItem = await $fetch(`/api/teams/${teamId}/sprocket-cogs`, {
  method: 'POST',
  body: {
    // ... fields (see schema)
  }
})
```

### PATCH /api/teams/{teamId}/sprocket-cogs/{cogId}

Update an existing item.

```typescript
const updated = await $fetch(`/api/teams/${teamId}/sprocket-cogs/${cogId}`, {
  method: 'PATCH',
  body: {
    // ... fields to update
  }
})
```

### DELETE /api/teams/{teamId}/sprocket-cogs/{cogId}

Delete an item.

```typescript
await $fetch(`/api/teams/${teamId}/sprocket-cogs/${cogId}`, {
  method: 'DELETE'
})
```

## Usage

### In Components

```vue
<script setup lang="ts">
// Query items
const { items: cogs, pending } = await useCollectionQuery('sprocketCogs')

// Mutations
const { create, update, deleteItems } = useCollectionMutation('sprocketCogs')
</script>

<template>
  <SprocketCogsList />
</template>
```

### Schema & Validation

```typescript
import { sprocketCogSchema } from './app/composables/useSprocketCogs'

// Validate data
const result = sprocketCogSchema.safeParse(data)
```

## Customization Guide

### Add a New Field

1. Add field to your schema JSON file
2. Regenerate: `crouton config --only cogs`
3. Or manually:
   - Add to `types.ts` interface
   - Add to composable schema and defaultValues
   - Add `<UFormField>` in Form.vue
   - Add column in composable if needed

### Customize Form Layout

Edit `app/components/Form.vue`:
- Add field groups with `meta.group` in schema
- Modify `CroutonFormLayout` slots
- Add custom field components

### Add Custom Validation

Edit `app/composables/useSprocketCogs.ts`:
```typescript
export const sprocketCogSchema = z.object({
  // ... existing fields
  email: z.string().email('Invalid email'),
  age: z.number().min(0).max(120)
})
```

### Add Server-Side Logic

Edit API endpoints in `server/api/teams/[id]/sprocket-cogs/`:
- Add validation with `readValidatedBody`
- Add custom business logic
- Emit custom events/hooks

---

*This README was auto-generated. Safe to modify - regeneration requires --force flag.*
